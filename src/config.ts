export const config = {
  autoplayNext: true,
  recentWindow: 5,
  screenTimeLimitMinutes: null as number | null,
  /** 0–100, applied to the YouTube player. iOS ignores it; use the device's own limits there. */
  maxVolume: 60,
};
